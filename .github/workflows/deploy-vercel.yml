name: Deploy Webapp to Vercel

on:
  push:
    branches: [main]
    paths:
      - 'language-tutor/webapp/**'
      - '.github/workflows/deploy-vercel.yml'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install and build
        working-directory: language-tutor/webapp
        run: |
          npm ci
          npm run build

      - name: Deploy to Vercel (CLI)
        if: ${{ secrets.VERCEL_TOKEN != '' }}
        working-directory: language-tutor/webapp
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
        run: |
          npx vercel pull --yes --environment=production --token="$VERCEL_TOKEN"
          npx vercel build --prod --token="$VERCEL_TOKEN"
          npx vercel deploy --prebuilt --prod --token="$VERCEL_TOKEN"

      - name: Trigger Vercel deploy hook (webapp-bay)
        if: ${{ secrets.VERCEL_TOKEN == '' && secrets.VERCEL_DEPLOY_HOOK != '' }}
        env:
          VERCEL_DEPLOY_HOOK: ${{ secrets.VERCEL_DEPLOY_HOOK }}
        run: |
          echo "Triggering Vercel deploy hook..."
          curl -fsS -X POST "$VERCEL_DEPLOY_HOOK"

      - name: No Vercel credentials
        if: ${{ secrets.VERCEL_TOKEN == '' && secrets.VERCEL_DEPLOY_HOOK == '' }}
        run: |
          echo "::warning::Set VERCEL_TOKEN or VERCEL_DEPLOY_HOOK in GitHub Secrets to deploy /voice"
          echo "Create a Deploy Hook in Vercel → webapp-bay project → Settings → Git → Deploy Hooks"
